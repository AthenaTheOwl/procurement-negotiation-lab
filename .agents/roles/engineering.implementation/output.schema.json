{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "engineering.implementation output",
  "description": "Shape the implementer writes when a patch is ready for review.",
  "type": "object",
  "required": ["patch_ref", "test_report_ref", "decision_ref", "requirement_ids"],
  "additionalProperties": false,
  "properties": {
    "patch_ref": {
      "type": "string",
      "description": "Path or URL to the diff artifact (factory-artifact path or git diff URL)."
    },
    "test_report_ref": {
      "type": "string",
      "description": "Path or URL to the test_report artifact."
    },
    "decision_ref": {
      "type": "string",
      "pattern": "^decisions/DEC-[A-Z]+-[0-9]{3,}-[a-z][a-z0-9-]*\\.md$",
      "description": "Path to the DEC file resolving the R-*."
    },
    "requirement_ids": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^R-[A-Z][A-Z0-9]*-\\d{3,}$"
      },
      "description": "R-* IDs the patch resolves."
    },
    "trace_id": {
      "type": "string",
      "description": "Factory trace_id when the run uses the factory; uuid hex otherwise."
    }
  }
}
