{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "product.spec-writer output",
  "description": "Shape the spec-writer writes when a spec ledger or delta is ready for review.",
  "type": "object",
  "required": ["spec_path", "requirement_ids", "files_touched"],
  "additionalProperties": false,
  "properties": {
    "spec_path": {
      "type": "string",
      "pattern": "^specs/\\d{4}-[a-z0-9][a-z0-9-]*/$",
      "description": "Directory path of the spec ledger, ending in a slash."
    },
    "requirement_ids": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^R-[A-Z][A-Z0-9]*-\\d{3,}$"
      },
      "description": "R-* IDs the spec defines."
    },
    "files_touched": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string"},
      "description": "Paths of the spec ledger files touched, relative to the repo root."
    },
    "delta_note": {
      "type": "string",
      "description": "Optional one-line summary of what changed since the prior spec version."
    }
  }
}
