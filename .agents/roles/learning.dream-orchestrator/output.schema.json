{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "learning.dream-orchestrator output",
  "description": "Shape the dream orchestrator writes when a weekly pass completes. The structured output.json itself follows dream-output.schema.json from athena-site; this role-level output names the pointer to that file plus a per-mode tally.",
  "type": "object",
  "required": ["week", "output_path", "report_path", "mode_tally"],
  "additionalProperties": false,
  "properties": {
    "week": {
      "type": "string",
      "pattern": "^\\d{4}-W\\d{2}$",
      "description": "ISO week label. Example: 2026-W22."
    },
    "output_path": {
      "type": "string",
      "pattern": "^dreams/\\d{4}-W\\d{2}/output\\.json$"
    },
    "report_path": {
      "type": "string",
      "pattern": "^dreams/\\d{4}-W\\d{2}/report\\.md$"
    },
    "mode_tally": {
      "type": "object",
      "description": "Number of candidates produced per dream mode.",
      "additionalProperties": {"type": "integer", "minimum": 0}
    },
    "candidate_count": {
      "type": "integer",
      "minimum": 0
    }
  }
}
