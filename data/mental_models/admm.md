# ADMM

ADMM is a coordination pattern. Each side solves its own local problem, then a
shared signal pulls the local answers toward one consensus quantity. Residual is
the disagreement left after an iteration.
