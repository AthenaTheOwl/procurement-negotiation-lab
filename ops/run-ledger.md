# run ledger

Append one row per spec pass. Use `pending` for the SHA before the commit is
created, then amend the row with the final SHA.

| Date | Spec pass | Commit | Evidence | Follow-ups |
|---|---|---|---|---|
| 2026-05-09 | 0003 Pass A - hero | `bc67c41` | `npm.cmd run build`; `npm.cmd run test -- --run`; `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest`; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin verified hero + source link + CTA; screenshot saved at `ops/qa-evidence/0003-pass-a-hero.png` | Pass B must replace the arc placeholder with the full 8-step arc. |
| 2026-05-09 | 0003 Pass C - TS formula engine | `81f6b70` | `npm.cmd run build`; `npm.cmd run test -- --run`; `npm.cmd run test -- web/src/model/formula.test.ts --run --coverage --coverage.include=web/src/model/formula.ts` (`formula.ts` 90.1% lines); `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest`; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin smoke reload verified hero and nav still render | Pass B9 must wire `compileFormula` into the authored-agent step. |
| 2026-05-09 | 0003 Pass B - guided arc | `8943bca` | `npm.cmd run build`; `npm.cmd run test -- --run`; `npm.cmd run test -- web/src/model/formula.test.ts --run --coverage --coverage.include=web/src/model/formula.ts`; `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest`; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin walked `/#arc` through steps 1-8, invalid formula handling, ADMM oscillation case, and CBT equal split; screenshot saved at `ops/qa-evidence/0003-pass-b-arc.png` | Pass D deploy and portfolio integration remain. |
| 2026-05-10 | 0004 Pass A-D - operational refinements | `9b787e4` | `npm.cmd run build`; `npm.cmd run test -- --run` (87 tests); `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest` (58 passed); `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin verified Lab alpha/reliability/epsilon/audit controls and Arc alpha/decoy/frontier additions; screenshots saved at `ops/qa-evidence/0004-pass-a-d-*.png` | Future spec 0005 should add multi-party/vendor portal mode; 0006 should add shareable run reports and replay artifacts. |
| 2026-05-11 | 0005 Pass A-E + 0006 + 0007 A/B/C/E + 0008 A-E | `59956bb` | `npx tsc --noEmit` (0 errors); `npm test -- --run` (135 tests across 18 files, +48 new); `npm run build` succeeds (482 KB main + 442 KB cytoscape chunk, gzip 147/142 KB); `python scripts/spec_check.py` passes with all 0005-0008 R-* IDs present. New zod-validated scenario schema (`scenarioSchema.ts`), N-party data model (`participants.ts`), 10-strategy library (`data/strategies.ts`), view picker + redaction (`views.ts`, `ViewPicker.tsx`, `ParticipantRoster.tsx`), Shapley/proportional/equal multi-party transfers (`shapleyTransfer.ts`, `MultiPartyTransferTable.tsx`). Run reports (`runReport.ts`, `runReportSchema.ts`, `reportStorage.ts`, `reportMarkdown.ts`, `RunReportPanel.tsx`) with localStorage ledger capped at 20. Bridges: CSV import Open-Contracting shape (`csvImport.ts`, `CSVImportPanel.tsx`), chip-supply-chain-map fetch with sessionStorage cache (`chipMap.ts`), supplier-risk-rag-agent JSONL fetch + risk-score derivation (`supplierRisk.ts`), `sourceProvenance.ts` with `ProvenanceBadge`. Source graph (`surfaces/arena/SourceGraph.tsx`) via lazy-loaded cytoscape. Production hardening: `factories.ts`, typed `decisionEvent.ts` log, `spec_check.py` extended to specs 0005-0008. | Spec 0007 D (Playwright smoke) and 0006 E (dedicated `/report` summary route) deferred to next pass. Browser QA + screenshots and bridge-failure visual verification pending. Pushed to branch `specs/0005-0008-implementation`; PR review before merge to main. |
| 2026-05-11 | 0005 + 0006 + 0007 + 0008 finish pass: scenarios + report route + Playwright + integration | `pending` | `npx tsc --noEmit` (0 errors); `npm test -- --run` (146 tests across 21 files, +11 over prior pass); `npm run build` succeeds (492 KB main + 442 KB cytoscape, gzip 150/142 KB); `python scripts/spec_check.py` OK. Five new lab scenarios authored (`advanced-packaging-bottleneck`, `export-control-shock`, `hyperscaler-surge`, `distributor-floats-inventory`, `audit-catches-misreport`) covering packager / logistics / distributor / multi-buyer / decoy-catches-misreport story arcs. `deriveParticipants` extended with preset-aware role rotation so multi-party presets actually instantiate packager + logistics + distributor strategies. Spec 0006 E ships: `ReportSurface` at `?report=<id>` and `?json=<encoded>`, read-only, screenshot-safe, links into RunReportPanel ledger as "open as page". Spec 0007 D ships: `playwright.config.ts`, `web/e2e/smoke.spec.ts` (6 smoke checks), and `.github/workflows/smoke.yml` (Monday 10:00 UTC + workflow_dispatch, opens issue on scheduled failure). Spec 0007 F ships: 3 integration tests in `web/src/integration/scenario-to-report.test.ts` covering scenario → ledger → report, CSV → derived participants → report, and chip-map provenance round-trip. | Browser QA against the five new presets + the Playwright workflow first run still pending. PR review then merge `specs/0005-0008-implementation` → main. |
