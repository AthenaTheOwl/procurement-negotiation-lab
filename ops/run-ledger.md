# run ledger

Append one row per spec pass. Use `pending` for the SHA before the commit is
created, then amend the row with the final SHA.

| Date | Spec pass | Commit | Evidence | Follow-ups |
|---|---|---|---|---|
| 2026-05-29 | 0009 Pass J - CI enforces run-evidence chain | `pending` + DEC commit | `python scripts/check_schema_cache_freshness.py` (8 schemas); `python scripts/voice_lint.py` (clean, 161 files); `python scripts/check_no_bom.py` (clean); `python scripts/spec_check.py` (14 active specs); `python scripts/validate_decisions.py` (30 decisions); `python scripts/validate_run_evidence.py` (9 events, 1 run record, 2 run_ids referenced); local replay smoke against canonical sample `run-7b662d3f68b1` (EQUIVALENT after `git checkout <recorded-sha>` + worktree-restore of finalized Run record); `python -m trace_to_eval evidence from-cdcp-events ops/event-ledger/run-7b662d3f68b1.jsonl --out /tmp/packet.json --portfolio-root e:/claude_code/random-apps` (8 events, 4 gate results); `python -m trace_to_eval evidence validate /tmp/packet.json` (matches v2-1 schema). Adds `.github/workflows/run-evidence-gates.yml` with sibling checkout of `AthenaTheOwl/trace-to-eval-harness`, packet-generation-from-canonical-sample + packet-validation gates, and a replay-smoke step that extracts the sandbox SHA from the canonical Run record's `sandbox_image_ref` URI, checks the recorded SHA out, restores the HEAD-finalized Run record into the worktree, and runs `scripts/replay_run.py --run-id run-7b662d3f68b1`. Registers DEC-FACTORY-011 + R-FACTORY-RUN-EVIDENCE-019..022. Updates `scripts/spec_check.py::REQUIRED_WORKFLOW_PROOFS` so a missing or renamed workflow file fails spec-check. | Round 7 Phase 2 followups: extend chip-supply-chain-map + ai-field-brief CI to the same contract; promote the worktree-restore trick to a small helper if a second product repo needs it. |
| 2026-05-29 | 0009 Pass I - portable repo:// URI migration | `9fa7400` + DEC commit | `python -m uv run pytest tests/factory/` (93 passed); `python scripts/voice_lint.py` (clean, 161 files); `python scripts/spec_check.py` (14 active specs); `python scripts/check_no_bom.py` (clean); `python scripts/check_schema_cache_freshness.py` (8 schemas); `python scripts/validate_decisions.py` (29 decisions); `python scripts/validate_roles.py` (8 roles); `python scripts/validate_tools.py` (12 tools); `python scripts/validate_policies.py` (6 policies); `python scripts/validate_skills.py` (1 skill); `python scripts/validate_dreams.py` (1 dream, 5 candidates); `python scripts/validate_run_evidence.py` (9 events, 1 run record, 2 run_ids referenced). Migrates emitter to the repo:// + artifact:// URIs from DEC-CDCP-014. New helpers `build_repo_uri`, `build_artifact_uri`, `resolve_uri`, `extract_repo_uri_sha`, `is_repo_uri`, `SANDBOX_PENDING_PLACEHOLDER` in `src/procurement_lab/run_evidence.py`. Pipeline now writes `workspace_id == 'procurement-negotiation-lab'`, repo:// URIs in `inputs[].ref`, and the PENDING placeholder for `sandbox_image_ref` (fixed by `scripts/finalize_sandbox_ref.py` after the regeneration commit lands). Validator + replay grow shared `resolve_uri`; replay's `_extract_recorded_sha` parses both URI and legacy forms. Regenerates the committed sample to `run-7b662d3f68b1` with the finalized `sandbox_image_ref`. Registers DEC-FACTORY-010 + R-FACTORY-RUN-EVIDENCE-015..018. | Next round: install CI gates for the URI scheme (Round 7); extend trace-to-eval packet consumer to read repo:// URIs in cross-repo run-evidence packets. |
| 2026-05-28 | 0009 Pass H - factory replay command | `c96c46d` | `python -m uv run pytest tests/factory/test_replay_run.py` (5 passed); `python -m uv run pytest` (141 passed); `python -m uv run python scripts/validate_run_evidence.py` (9 events, 1 run record); `python -m uv run python scripts/validate_decisions.py` (28 decisions); `python -m uv run python scripts/voice_lint.py` (clean); `python -m uv run ruff check .` (1 pre-existing E501 on pipeline.py:1021); `python -m uv run mypy src` (Success); `python scripts/replay_run.py --run-id run-16a7bf515611` (EQUIVALENT). Adds `scripts/replay_run.py` + `tests/factory/test_replay_run.py`, refreshes sample sandbox_image_ref to current HEAD, commits first replay artifact at `ops/event-ledger/replay-run-16a7bf515611-2026-05-28T12-23-12Z.jsonl` + `ops/replay-records/run-16a7bf515611/05c783cb-b12d-427c-beb2-2a77c34cd339.json`, registers DEC-FACTORY-009 + R-FACTORY-RUN-EVIDENCE-011..014. | Next pass: extend equivalence-replay to the trace-to-eval packet consumer so the cross-repo packet validates against the freshened sample. |
| 2026-05-25 | 0009 Pass E - factory console | `73e5881` | `python -m uv run python scripts/spec_check.py`; `python -m uv run python scripts/voice_lint.py`; `python -m uv run python scripts/check_no_bom.py`; `python -m uv run python scripts/validate_decisions.py`; `python -m uv run python scripts/validate_roles.py`; `python -m uv run python scripts/validate_tools.py`; `python -m uv run python scripts/validate_policies.py`; `python -m uv run python scripts/validate_skills.py`; `python -m uv run python scripts/validate_dreams.py`; `python -m uv run pytest` (102 passed); `npm.cmd run lint`; `npm.cmd run test` (engine 229 passed, web 229 passed); `npm.cmd run build`. | Next pass: add hosted MCP or event-log schema coverage only after a runtime boundary is chosen. |
| 2026-05-09 | 0003 Pass A - hero | `bc67c41` | `npm.cmd run build`; `npm.cmd run test -- --run`; `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest`; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin verified hero + source link + CTA; screenshot saved at `ops/qa-evidence/0003-pass-a-hero.png` | Pass B must replace the arc placeholder with the full 8-step arc. |
| 2026-05-09 | 0003 Pass C - TS formula engine | `81f6b70` | `npm.cmd run build`; `npm.cmd run test -- --run`; `npm.cmd run test -- web/src/model/formula.test.ts --run --coverage --coverage.include=web/src/model/formula.ts` (`formula.ts` 90.1% lines); `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest`; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin smoke reload verified hero and nav still render | Pass B9 must wire `compileFormula` into the authored-agent step. |
| 2026-05-09 | 0003 Pass B - guided arc | `8943bca` | `npm.cmd run build`; `npm.cmd run test -- --run`; `npm.cmd run test -- web/src/model/formula.test.ts --run --coverage --coverage.include=web/src/model/formula.ts`; `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest`; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin walked `/#arc` through steps 1-8, invalid formula handling, ADMM oscillation case, and CBT equal split; screenshot saved at `ops/qa-evidence/0003-pass-b-arc.png` | Pass D deploy and portfolio integration remain. |
| 2026-05-10 | 0004 Pass A-D - operational refinements | `9b787e4` | `npm.cmd run build`; `npm.cmd run test -- --run` (87 tests); `python -m uv run python scripts/spec_check.py`; `python -m uv run pytest` (58 passed); `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; Browser plugin verified Lab alpha/reliability/epsilon/audit controls and Arc alpha/decoy/frontier additions; screenshots saved at `ops/qa-evidence/0004-pass-a-d-*.png` | Future spec 0005 should add multi-party/vendor portal mode; 0006 should add shareable run reports and replay artifacts. |
| 2026-05-12 | Factory v1: in-repo orchestrator | `ff27e83` | Factory scaffold added under `scripts/factory/` with task YAML loader, worktree setup, worker abstraction, gate runner, SQLite state, and dry-run support. | Factory v2 needed checkpoints, artifacts, traces, and real CLI correlation. |
| 2026-05-12 | Factory v2: managed runtime | `26098d2` | `python -m uv run pytest tests/factory/` (25/25); `python -m uv run pytest` (83/83); `npx.cmd tsc --noEmit`; `npm.cmd test -- --run` (160/160); `python scripts/spec_check.py` OK. Added real checkpoints, trace IDs, artifact refs, schema migrations, and CLI metadata fields. | MCP exposure, spec expansion, dual review, metadata parser hardening, and parallel routing moved to spec 0009. |
| 2026-05-14 | 0009 factory dev control plane | `b08270b` | `python -m uv run pytest tests/factory/` (34 passed); `python -m uv run pytest` (92 passed); `npx.cmd tsc --noEmit`; `npm.cmd test -- --run` (160 passed); `python scripts/spec_check.py` OK; `python -m uv run ruff check .`; `python -m uv run mypy src`; `python -m uv run bandit -q -r src`; `python -m uv run pip-audit`; `npm.cmd run build`. Added MCP-compatible stdio server, spec-to-task expansion, bounded dual review, JSON/JSONL CLI metadata parsing, and optional LangGraph router/fallback. | Merge branch to `main`, push, verify production. |
| 2026-05-11 | 0005 Pass A-E + 0006 + 0007 A/B/C/E + 0008 A-E | `59956bb` | `npx tsc --noEmit` (0 errors); `npm test -- --run` (135 tests across 18 files, +48 new); `npm run build` succeeds (482 KB main + 442 KB cytoscape chunk, gzip 147/142 KB); `python scripts/spec_check.py` passes with all 0005-0008 R-* IDs present. New zod-validated scenario schema (`scenarioSchema.ts`), N-party data model (`participants.ts`), 10-strategy library (`data/strategies.ts`), view picker + redaction (`views.ts`, `ViewPicker.tsx`, `ParticipantRoster.tsx`), Shapley/proportional/equal multi-party transfers (`shapleyTransfer.ts`, `MultiPartyTransferTable.tsx`). Run reports (`runReport.ts`, `runReportSchema.ts`, `reportStorage.ts`, `reportMarkdown.ts`, `RunReportPanel.tsx`) with localStorage ledger capped at 20. Bridges: CSV import Open-Contracting shape (`csvImport.ts`, `CSVImportPanel.tsx`), chip-supply-chain-map fetch with sessionStorage cache (`chipMap.ts`), supplier-risk-rag-agent JSONL fetch + risk-score derivation (`supplierRisk.ts`), `sourceProvenance.ts` with `ProvenanceBadge`. Source graph (`surfaces/arena/SourceGraph.tsx`) via lazy-loaded cytoscape. Production hardening: `factories.ts`, typed `decisionEvent.ts` log, `spec_check.py` extended to specs 0005-0008. | Spec 0007 D (Playwright smoke) and 0006 E (dedicated `/report` summary route) deferred to next pass. Browser QA + screenshots and bridge-failure visual verification pending. Pushed to branch `specs/0005-0008-implementation`; PR review before merge to main. |
| 2026-05-11 | 0005 + 0006 + 0007 + 0008 finish pass: scenarios + report route + Playwright + integration | `25436db` | `npx tsc --noEmit` (0 errors); `npm test -- --run` (146 tests across 21 files, +11 over prior pass); `npm run build` succeeds (492 KB main + 442 KB cytoscape, gzip 150/142 KB); `python scripts/spec_check.py` OK. Five new lab scenarios authored (`advanced-packaging-bottleneck`, `export-control-shock`, `hyperscaler-surge`, `distributor-floats-inventory`, `audit-catches-misreport`) covering packager / logistics / distributor / multi-buyer / decoy-catches-misreport story arcs. `deriveParticipants` extended with preset-aware role rotation so multi-party presets actually instantiate packager + logistics + distributor strategies. Spec 0006 E ships: `ReportSurface` at `?report=<id>` and `?json=<encoded>`, read-only, screenshot-safe, links into RunReportPanel ledger as "open as page". Spec 0007 D ships: `playwright.config.ts`, `web/e2e/smoke.spec.ts` (6 smoke checks), and `.github/workflows/smoke.yml` (Monday 10:00 UTC + workflow_dispatch, opens issue on scheduled failure). Spec 0007 F ships: 3 integration tests in `web/src/integration/scenario-to-report.test.ts` covering scenario â†’ ledger â†’ report, CSV â†’ derived participants â†’ report, and chip-map provenance round-trip. | Browser QA against the five new presets + the Playwright workflow first run still pending. PR review then merge `specs/0005-0008-implementation` â†’ main. |
| 2026-05-11 | 0005 + 0008 acceptance backfill: participant builder, scenario JSON IO, example CSV, public-data-boundary | `e7f8331` | `npx tsc --noEmit` (0 errors); `npm test -- --run` (160 tests across 25 files, +14 since prior pass across 4 new test files); `npm run build` succeeds (499 KB main + 442 KB cytoscape, gzip 152/142 KB); `python scripts/spec_check.py` OK. New `ParticipantBuilder` component closes R-PORTAL-004 acceptance: filter by role + one-click instantiate any of the 10 strategies + remove (min 2, max 8, disabled appropriately). New `ScenarioImportExportPanel` closes R-PORTAL-005 acceptance: paste-JSON load with zod-validated field-path errors, copy-as-JSON to clipboard, automatic migration of pre-0.5.0 shapes with per-field warning summary via `scenarioMigrate.ts`. New `data/example-imports/open-contracting-sample.csv` (9-row Open-Contracting-shape file with substrate buyers/suppliers/packagers) closes R-BRIDGE-001 acceptance and is exercised in `exampleCsv.test.ts` against `parseImport`. `docs/public-data-boundary.md` extended with explicit bridge rules (allowed/disallowed fetches, CSV-import boundary, provenance taxonomy, out-of-scope items) closing R-BRIDGE-005 acceptance. | Browser QA pending. After QA + Playwright first run, merge `specs/0005-0008-implementation` â†’ main and amend SHAs into R-SPEC-005/6/7/8 rows. |
